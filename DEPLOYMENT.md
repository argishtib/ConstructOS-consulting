# Deployment — ConstructOS Website

This is a **static site**. It is deployed via **GitHub Pages** from the `docs/` folder.

**Deploy steps and options (custom domain, Formspree, etc.):** see **docs/GITHUB-PAGES.md**.

No server or Python runtime is required for deployment.
